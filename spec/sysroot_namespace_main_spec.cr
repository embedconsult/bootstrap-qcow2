require "./spec_helper"
require "../src/sysroot_namespace_main_lib"

describe Bootstrap::SysrootNamespaceMain do
  pending "parses arguments and enters namespaces (requires unprivileged namespaces and would exec a new process)" do
  end
end
